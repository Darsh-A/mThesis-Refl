# Taste
- Prefers one-off/scratch files (scripts, generated plots, and other transient artifacts) to be written under `/tmp/commandcode/*` rather than inside the project repository, reserving the repo for permanent-use files only. Confidence: 0.95
- Values rigorous, evidence-backed evaluation of ML results: questions headline metrics (R², feature importance), expects diagnostics like overfitting checks and permutation importance, and runs sensitivity/ablation experiments (e.g. dropping an element and re-training, then repeating with a different element to compare outcomes) rather than taking results at face value. Also prefers validating a methodological/approximation change as a side-by-side ablation (regenerate a small side dataset with the alternative, retrain, compare feature importances/R² against current results) before committing, rather than silently replacing one approximation with another without evidence it moves conclusions. Confidence: 0.95
- Prefers generated diagnostic plots/figures saved into a dedicated subfolder within the relevant project module (e.g. `src/randomForest/diagnostic/`) rather than left in /tmp or the repo root. Confidence: 0.8
- Prefers cheap in-place edits to existing cached/generated data (e.g. dropping columns from a saved dataset) over regenerating data from scratch, when the transformation is equivalent. Confidence: 0.9
- Does not want generation/source code (e.g. `dataset.py`) edited during experiments; instead operate on the already-generated dataset for training/eval (e.g. drop columns from the cached data) and leave the generator untouched. Confidence: 0.8
- When facing a fundamental methodological blocker (e.g. no observed/labeled data available), wants to step back for strategic guidance — a reframing of the problem plus a prioritized, ordered plan of next steps — rather than jumping straight into implementation. Confidence: 0.6
- Prefers keeping existing code intact for reference when adding a new approach, writing new implementations alongside the old ones (e.g. a separate `train_quantile_rf.py` next to `train_rf.py`) rather than deleting or replacing. Confidence: 0.85
- Prefers continuous interpolation across tabulated data grids (e.g. metallicity/yields between grid points) over snapping to the nearest grid value, and wants that behavior applied consistently to all analogous sources and parallel code paths — after one branch is made continuous, immediately checks whether sibling branches (e.g. the combined PISN+SN path) still snap; when a tabulated path can't interpolate (e.g. a lifetime lookup that snaps to integer [Fe/H]), prefers swapping in an analytic, continuous formula (e.g. Raiteri 1996 lifetime) rather than accepting grid snapping. Confidence: 0.95
- Prefers cross-source generalization evaluation for ML models: train on one yield source's dataset and validate on a separate, held-out source (e.g. train on Limongi18, eval on a freshly generated smaller Nomoto13 dataset), rather than relying only on same-source held-out splits. Confidence: 0.7
- Communicates requested analyses as formal specifications: writes out the mathematical definition (LaTeX) plus a concrete worked example, and expects the implementation to follow that spec faithfully. Confidence: 0.7
- Prefers model interpretability at the level of physically meaningful quantities — e.g. element-level importance by aggregating a model's derived features (pairwise ratios) back to their underlying elements (shuffle all ratios containing an element and measure the R²/accuracy drop) — rather than stopping at raw feature importance. Confidence: 0.7
- Prefers persisting derived/split datasets as separate named cache files (e.g. `dataset_pisn_high.npz` / `dataset_pisn_low.npz`) so each can be trained/evaluated independently and reused, rather than re-filtering in-memory each run. Confidence: 0.6
- Prefers consistent evaluation reporting across experiments — when asked to "show results like before," reuse the same metric table (train/test/eval R², RMSE, corr, 68%/90% CI coverage, pinball) so runs are directly comparable. Confidence: 0.6
- Prefers log-space interpolation (linearly interpolating log10(value), then exponentiating) for tabulated quantities that span multiple orders of magnitude or have steep onsets/jumps (e.g. Cu, Zn yields), rather than linear interpolation, which over/under-estimates transition-region values. Confidence: 0.7
- Cross-checks generated/synthetic data against the physical limits of the underlying source models (e.g. whether the enrichment [Fe/H] range exceeds the Limongi/Nomoto SN yield grids), and expects apparent contradictions to be reconciled with concrete computed numbers rather than hand-waved. Confidence: 0.6
- When a reported number or table is challenged, wants the exact code path/function that produced it identified (e.g. which function generated `[Fe/H]`) rather than trusting a labeled summary, and wants derived quantities validated against the primary source's actual equations (e.g. Salvadori's per-element Eq. 12) rather than simplified proxies (e.g. solar-scaled total-Z). Confidence: 0.7
- When a genuine bug in the repo's source code is confirmed, wants it fixed directly in the codebase rather than merely worked around in temp scripts or left as a written recommendation. Confidence: 0.6
- Prefers load-data functions to merge multiple source tables into a single combined output rather than returning them separately. Confidence: 0.6
- Prefers evaluating/validating ML models against real observational data (e.g. an observed stellar-abundance catalogue) rather than only on synthetic theoretical yields (e.g. Nomoto yields), when an observed sample is available. Confidence: 0.7
- Keeps thesis research in an Obsidian-style Markdown vault (`/home/ardo/mlem/Vaults/Thesis/`, with paper summaries under `thesis_vault/Summaries/`), and when asking conceptual questions points at those notes so the assistant reads and cross-references them against the code. Confidence: 0.7
- When asking for an explanation of a pipeline/formula, wants the physical meaning of every ambiguous symbol pinned down (e.g. which mass — initial/ZAMS vs turnoff — and which metallicity) and the full chain traced through the code by function and file:line, rather than a high-level restatement. Confidence: 0.6
- When adding new functionality, wants it placed in the appropriate existing module by responsibility (e.g. dataset-generation entry points in `dataset.py`, supporting helpers/formulas in the relevant `utils`/`formulas` files) rather than in a single monolithic file or a throwaway script. Confidence: 0.7
- Prefers consolidating related generation variants into a single entry point gated by a parameter/sentinel value (e.g. folding the pure-SNII case into `generate` via `f_pisn == 0`) rather than maintaining a separate parallel function. Confidence: 0.6
- Prefers visible summary/diagnostic output after dataset generation — e.g. printing the yield sources used and a count of samples in each f_pisn bin (0, 0.1, …, 1.0) — rather than silently returning arrays, so the composition of the generated data is immediately inspectable. Confidence: 0.6
- Prefers reusing existing data-loading/helper functions in the repo (e.g. `load_obs_li`) rather than reimplementing parsers or reading raw files directly, when such functions already exist. Confidence: 0.7
- For ML regression tasks, wants both a standard/point random forest and a quantile random forest trained and evaluated (point predictions plus predictive uncertainty/CI), rather than only one model type. Confidence: 0.7
